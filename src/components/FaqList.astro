---
interface Item {
  q: string;
  a: string;
}
interface Props {
  items: Item[];
}
const { items } = Astro.props;
---

{
  items.map((item) => (
    <details class="faq">
      <summary>{item.q}</summary>
      <div class="prose" set:html={item.a} />
    </details>
  ))
}
