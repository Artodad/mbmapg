---
interface Props {
  title: string;
  lede?: string;
}
const { title, lede } = Astro.props;
---

<section class="page-hero">
  <div class="wrap">
    <h1>{title}</h1>
    {lede && <p>{lede}</p>}
  </div>
</section>
